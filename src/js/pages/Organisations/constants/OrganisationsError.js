/**
 * These are used as exception messages for the organisations component
 * be aware that these keys are also mapped to lokalise to load the translations
 */
const OrganisationsError = {
    ALL_FIELDS_REQUIRED: 'organisations_all_fields_required',
    UNEXPECTED_ERROR: 'organisations_unexpected_error'
};

export default OrganisationsError;
